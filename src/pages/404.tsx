import { NotFoundMain } from '@/ui/pages/not-found';
import React from 'react';

/**
 *  ? Not Found Page
 *  ? - Page to handle route that is not available. (this is static)
 */
const NotFound: React.FC = () => {
	return <NotFoundMain />;
};

export default NotFound;
